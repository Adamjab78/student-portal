
import { Text, View, StyleSheet } from "react-native";

const styles = StyleSheet.create({
  header: {
    flex: 1,
    flexDirection: "row",
    marginHorizontal: 10,
    borderBottomWidth: 1,
    borderColor: "grey",
  },
});

export default styles;
